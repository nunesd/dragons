import styled from "styled-components";

import {
  flexCenter,
  FullContainer,
} from "components/shared/CenteredContainers";

export const DragonContainer = styled(FullContainer)`
  ${flexCenter}
`;
